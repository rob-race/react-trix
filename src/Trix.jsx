import React, { Component, PropTypes } from 'react'

class Trix extends React.Component {
  static propTypes = {
  }
  static defaultProps = {
  }
  render() {
    return <div></div>
  }
}

export default Trix